// import routesConfig from 'app/licitame-configs/routesConfig';

const initialState = {};

const routes = (state = initialState, action) => {
	switch (action.type) {
		default: {
			return state;
		}
	}
};

export default routes;
